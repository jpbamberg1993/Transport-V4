<% provide(:title, 'Select Carriers') %>
<h1>Select carriers for this shipment</h1>

<!--<div class="row">
  <div class="col-md-4 col-md-offset-0">
    <%= form_for @shipment do |f| %>
      <br>
      <ul>
        <%= f.fields_for :user_shipments do |user_shipment_form| %>
          <% @carriers.each do |carrier| %>
            <li>
              <%= carrier.company_name %>
              <% user_id = carrier.id %>
              <%= user_shipment_form.check_box("user_id", {:multiple => true}, user_id, nil) %>
            </li>
          <% end %>
        <% end %>
      </ul>
      <%= f.submit "Select carriers", class: 'btn btn-primary' %>
    <% end %>
  </div>
</div>-->

<div class="row">
  <div class="col-md-4 col-md-offset-0">
    <%= form_for @shipment do |f| %>
      <br>
        <code><ul><span class="code-comment"></span>
          <%= f.fields_for :user_shipments do |user_shipment_form| %>
            <% @carriers.each do |carrier| %>
              <li class="choose">
                <%= carrier.company_name %>
                <% user_id = carrier.id %>
                <%= user_shipment_form.check_box("user_id", {:multiple => true}, user_id, nil) %>
              </li>
            <% end %>
          <% end %>
        </ul></code>
      <%= f.submit "Select carriers", class: 'btn btn-primary' %>
    <% end %>
  </div>
</div>
